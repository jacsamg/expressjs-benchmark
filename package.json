{
  "name": "expressjs-benchmark",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "start:v4": "npm start --prefix ./v4",
    "start:v5": "npm start --prefix ./v5",
    "start:connect": "npm start --prefix ./connect",
    "benchmark:connect": "autocannon -c 100 -d 30 http://localhost:3000/",
    "benchmark:v4": "autocannon -c 100 -d 30 http://localhost:3004/",
    "benchmark:v5": "autocannon -c 100 -d 30 http://localhost:3005/"
  },
  "devDependencies": {
    "autocannon": "^7.15.0"
  }
}